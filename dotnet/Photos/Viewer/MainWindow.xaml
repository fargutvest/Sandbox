<Window x:Class="Viewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{StaticResource MainWindowViewModel}"
        Title="Viewer" Height="450" Width="800">
    <Grid x:Name="MainGrid"  Focusable="True" PreviewKeyUp="On_KeyDown" Loaded="On_Loaded" Background="Gray">
        <Image Margin="10" Source="{Binding ImageFilePath}" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
        <Label Foreground="Yellow" FontSize="20" Content="{Binding Tags}"/>
    </Grid>
</Window>
